{
  "build_status": "READY",
  "backend_version": "R12",
  "bundle": "R12-UI-20260812-v2",
  "version": "R12",
  "route": "/backend/v1/integracao/ac/run-round-2d2a-r12",
  "method": "POST",
  "execution_enabled": true,
  "button_enabled": true,
  "executed": false,
  "server_side_lock": "armed",
  "activecampaign_calls": 0,
  "binding_verified": true,
  "initial_state": "ready",
  "server_side_lock_details": {
    "armed": true,
    "mechanism": "com_parametros key=ac_r12_execution_lock",
    "persists_across_restarts": true,
    "engaged_on_first_call": true,
    "blocks_re_execution": true,
    "applies_on_pass": true,
    "applies_on_fail": true,
    "applies_on_blocked": true,
    "applies_on_error": true
  },
  "tests_included": [
    "1_disabled_returns_503",
    "2_wrong_method_get",
    "3_invalid_content_type",
    "4_empty_body",
    "5_malformed_body",
    "6_oversized_body",
    "7_missing_signature",
    "8_invalid_signature"
  ],
  "webhook_flag_final_state": "false",
  "webhook_fix": {
    "missing_signature": "Returns e.json(401, {error:'missing_signature', message:'Assinatura ausente'}) — same mechanism as 503/405 branches",
    "invalid_signature": "Returns e.json(401, {error:'invalid_signature', message:'Assinatura invalida'}) — both length check and comparison use e.json, no e.unauthorizedError",
    "mechanism": "Both 401 branches use e.json() exactly like the JSON 503 and 405 branches — no throw, no BadRequest, no raw text 400"
  },
  "r11_lock_preserved": true,
  "r11_lock_mechanism": "com_parametros key=ac_r11_execution_lock — untouched, remains consumed and permanently locked",
  "untouched_scope": [
    "webhook",
    "signature_fix",
    "runner",
    "test_matrix",
    "payloads",
    "R12EvidenceBlock",
    "IntegrationTestsTab",
    "bundle",
    "migrations",
    "rbac",
    "business_collections",
    "secrets"
  ],
  "note": "R12 execution gate is ENABLED. Server-side single-execution lock is armed via com_parametros key=ac_r12_execution_lock. The route is ready for a future single manual run. No tests executed during this change — executed:false. The lock will engage on the first authenticated POST and block all subsequent calls regardless of outcome (PASS, FAIL, BLOCKED, or error)."
}
